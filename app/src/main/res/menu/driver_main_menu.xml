<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/driver_profile"
        android:title="Profile"
        app:showAsAction="never"/>

    <item android:id="@+id/driver_notifications"
        android:title="Notifications"
        app:showAsAction="never"/>

    <item android:id="@+id/driver_messages"
        android:title="Messages"
        app:showAsAction="never"/>

    <item android:id="@+id/assigned_children"
        android:title="Assigned Children"
        app:showAsAction="never"/>

     <item android:id="@+id/driver_logout"
        android:title="Logout"
        app:showAsAction="never"/>

</menu>